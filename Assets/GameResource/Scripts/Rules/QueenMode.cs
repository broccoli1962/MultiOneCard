namespace Game.Rules
{
    /// <summary>
    /// Q 모드. Reverse=방향 반전(2인이면 상대 스킵), Give=지급 체인(queenStack=1).
    /// </summary>
    public enum QueenMode
    {
        Reverse = 0,
        Give,
    }
}
